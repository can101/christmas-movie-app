import React, { FC, ReactElement, Fragment } from 'react'

const Carousel: FC = (): ReactElement => {
    return (
        <div style={{background:"blue",height:500}}>
            <span>Carousel</span>
        </div>
    )
}

export default Carousel
